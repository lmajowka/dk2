<div class="container">
  <div class="page-header">
    <h1><%= @level.name %></h1>
    <div class="level-actions">
      <%= link_to "← Back", levels_path, class: "btn btn-secondary" %>
      <%= link_to "Edit", edit_level_path(@level), class: "btn btn-primary" %>
    </div>
  </div>

  <canvas
  width="800"
  height="450"
  style="display: block; border: 2px solid red"
  data-controller="player"
  data-player-background-url-value="<%= asset_path("fundo.jpg") %>"
  data-player-sprite-url-value="<%= asset_path("seya.gif") %>"
  data-player-punch-url-value="<%= asset_path("seya_soco.gif") %>"
  data-player-tile-urls-value="<%= tile_urls_json %>"
  data-player-goal-tile-id-value="<%= goal_tile_id %>"
  data-player-map-value='<%= @level.map.to_json %>'
  data-player-props-value='<%= @level.props.to_json %>'
  data-player-prop-urls-value="<%= prop_urls_json %>"
  data-player-enemies-value='<%= @level.enemies.to_json %>'
  data-player-enemy-url-value="<%= asset_path('enemy.gif') %>"></canvas>
</div>
